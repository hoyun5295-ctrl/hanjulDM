# 15. 제작 화면 재설계 + 템플릿 갤러리 복원 (2026-08-20)

> **이 문서가 소유하는 것** = 이 축의 접수·원인·디자인 방향·구현·잔여·⛔.
> 템플릿 값의 SoT = `utils/flyer/config/flyer-business-types.ts`(TEMPLATE_REGISTRY) · 렌더 = `flyer-templates.ts`(CT-F14).

---

## §1 접수 (Harold, 2026-08-20)

1. 제작 화면 첫 화면이 카드 3개뿐이라 썰렁하고, 카드 안 문구 줄바꿈이 깨져 있다.
2. 두 번째 화면(제작)이 AI 전단 솔루션으로 보이지 않는다. 디자인 스킬을 안 썼다.
3. **"그전에 있던 좋은 템플릿들 다 어디에 갔다버리고 오히려 퇴보했냐"** — 템플릿 선택이 사라졌다.
4. 압도적인 솔루션으로, 화려하게. 템플릿도 추가해서.

---

## §2 원인 (사실)

**템플릿은 버려지지 않았다.** `TEMPLATE_REGISTRY`에 10종이 label·desc·color(그라데이션)까지 갖춘 채 살아 있고,
`GET /api/flyer/business-types/templates`도 그대로 응답한다. 렌더 엔진도 11개 함수 모두 존재한다.
**W3(0820)에서 "AI 자동 선정" 하나로 좁히면서 고르는 UI만 삭제**한 것이 퇴보의 실체다.
게다가 `POST /auto-build`가 항상 추천값으로 덮어써서, 화면이 고르려 해도 서버가 받아주지 않았다.

줄바꿈 깨짐 = 한글 `word-break` 미지정(단어 중간에서 끊김) + 카드 문구가 길었다.

---

## §3 디자인 방향 — 인쇄소 카운터(종이·잉크·가격표)

만드는 물건이 전단이니 화면도 전단의 언어를 쓴다. 자매 제품(다크 SaaS 톤)을 베끼지 않는다.

| 축 | 결정 |
|---|---|
| 타이포 | ~~헤드라인 = Black Han Sans / 본문 = Noto Sans KR~~ → **0821 번복(Harold): 전면 Pretendard 단일, 계층은 weight 900~400** (§8) |
| 색 | 종이(#f8fafc·surface) + 잉크(slate-900) + 행사 오렌지(brand) 지배색. 인디고(primary)는 시스템 액션에만 |
| 질감 | 미세 격자 종이(`.paper`), 잉크 그라데이션 텍스트(`.ink-gradient`), 광택 스켈레톤(`.sheen`) |
| 모션 | 진입 순차 노출(`.rise`, 70ms 간격) · 살아있는 점(`.pulse-dot`) · `prefers-reduced-motion` 존중 |
| 한글 | `.kr`(word-break: keep-all) — 줄바꿈 깨짐의 근본 처방 |

토큰은 이 프로젝트의 라이트 테마 실존 토큰만 쓴다(다크 클래스 금지 — LESSONS 상단 사고).

---

## §4 구현

**홈** — 히어로(AI 자동 구성 배지 + 대형 헤드 + 3버튼 + 지표 4) · 프리셋 3카드(등장 애니메이션·태그·메타, 문구 축약) ·
「이렇게 만들어집니다」 3단계 · 최근 전단 2열 카드(상태 배지·이어 만들기·URL 복사) + 빈 상태 카드.

**제작** — sticky 툴바(상품 담기 4소스 + 담긴 수 + 다른 느낌으로) ·
**AI 구성 스트립**(구성 중/완료 표시 · 사용 중인 템플릿 이름 · `AI 추천`/`직접 고름` 구분 · 판정 근거 칩) ·
종이 프레임 미리보기(빈 상태 = 안내 + 1클릭 담기 유도, 구성 중 = 상단 광택 바) · Source caption(미리보기 = 발행본) ·
라벨 붙은 행사 정보 · 담긴 상품 손질 · 발행 바.

**템플릿 갤러리(복원)** — 「디자인 바꾸기」 → 모달. `AI가 알아서`(기본) + 전 템플릿 카드.
각 카드는 레지스트리의 그라데이션을 그대로 견본으로 쓰고 label·desc를 싣는다. 누르면 즉시 재구성(1클릭).

**서버** — `POST /auto-build`가 `template`을 받으면 그 엔진으로 고정(레지스트리 화이트리스트),
`recommended_template`·`picked`를 함께 내려 화면이 추천과 선택을 구분해 표시한다.

**신규 엔진 2종**
| 코드 | 이름 | 성격 |
|---|---|---|
| `market_board` | 시장 대자보 | 크림 갱지 + 붉은 인장 + 점선 리더. **사진이 없어도 성립** — 정육·수산·명절 대목용 |
| `fresh_daily` | 신선 데일리 | 딥그린 + 큰 사진 + 원산지·규격 칩. 대표 상품 1 + 2열 그리드 — 신선 코너용 |

→ 선택 가능 템플릿 **10종 → 12종**.

---

## §5 검증

- backend tsc 0 · frontend/admin-frontend build 성공
- vitest 65건 통과. 실렌더 스모크를 **12엔진 × 수량 3밴드 × 이미지 유무 = 72조합**으로 확장해 신규 2종 포함 통과
- 신규 엔진 실데이터 렌더 확인 — market_board 8,482바이트 / fresh_daily 9,369바이트, 상품명·가격·카테고리·원산지·밴드·매체 CSS 주입 전부 확인

---

## §5-1 배포 후 실사용에서 잡힌 결함 3건 (2026-08-20, Harold 접수)

| 증상 | 원인 | 처방 |
|---|---|---|
| 엑셀로 담아도 변화 없음 | 모달은 `productName`을 주는데 새 제작 화면의 `addFromExcel`만 `name`을 읽어 **전 행이 이름 없음으로 걸러짐**(0건). 같은 모달을 쓰는 POP·인쇄 화면은 `productName`을 정상 사용 — W3 재작성 때 이 경로만 어긋났다 | 필드 정정 + 이미지 URL 승계 + **0건이면 사유 모달**(조용한 실패 금지) |
| 미리보기가 깨짐(글자 겹침·잘림) | 미리보기 프레임이 컨테이너 폭에 맞춰 **1배를 넘겨 확대**. 재설계로 좌측 컬럼이 넓어지자 393px 모바일 HTML이 ~1.6배로 부풀었다 | `Math.min(1, …)`로 확대 금지 + 393px 고정폭으로 가운데 세우고 바닥 종이 처리 |
| 상품 이미지 깨짐 | 미리보기는 `blob:` iframe이라 상대경로(`/api/flyer/...`)가 blob 출처로 풀려 전부 404(발행 페이지는 실제 도메인이라 멀쩡했다) | preview-html이 요청 출처를 `<base href>`로 주입 |

추가로 내 신규 엔진 2종이 **CT가 실제로 돌려주지 않는 계급 클래스**(`.long`·`.p-lg`)를 쓰고 있던 것을 게이트 작성 중 발견해 `nm-*`·`pr-*`로 정정했다.
셋 다 게이트 5로 고정(총 70건).

## §5-2 상품 이미지 1클릭 자동 부착 (2026-08-20, Harold 지시로 ⛔ 1건 완화)

W3 §3에서 **네이버 자동 확정 금지**로 막았던 축을 **웹 전단 한정으로 연다**. 막았던 이유가 "이미지검색 결과가 인쇄물로 나가는 구조"였으므로, 그 이유가 닿지 않는 범위만 연다.

- 신설 `POST /api/flyer/flyers/auto-images` — 이미지 없는 상품만 대상(최대 60), 화면은 버튼 1개.
- 여는 대신 지키는 3가지: **① 신뢰도 게이트 통과분만**(정제 토큰 전부 일치) **② 받아서 로컬 저장**(핫링크 0) **③ 출처 `naver` 표기**.
- 화면은 부착분을 `imageSource='네이버'`로 남기고, **인쇄 관문의 자동 이미지 동의 체크가 이 값으로 걸린다**(인쇄는 여전히 동의 없이 못 나간다).
- 429(일일 쿼터 소진)는 중단하고 사장님에게 그대로 알린다. 못 찾은 건은 이유와 함께 요약한다.
- 인쇄 파이프라인의 네이버 소싱 0은 그대로 유지.

게이트 4를 이 정책으로 갱신했다(총 72건). 옛 문구 "자동 확정 금지"는 더 이상 사실이 아니므로 테스트 주석도 함께 고쳤다.

⛔ 남는 경계 — **인쇄 발주에는 네이버 출처 이미지를 동의 없이 태우지 않는다.** 이 한 줄이 무너지면 처음 막았던 이유가 그대로 돌아온다.

**용도 확정(Harold 0820)** — 이 자동 부착은 **시연용**이다. 실제 고객사 납품 시에는 업체 보유 이미지 유무를 체크해 그쪽을 우선하는 로직으로 전환한다(그때 auto-images의 소스 우선순위만 갈아끼움 — 화면·게이트 구조는 유지).

## §6 잔여

- 실측: 홈 → 프리셋 → 상품 담기 → 디자인 바꾸기 12종 순회 → 발행 → POP·인쇄
- 신규 2종 육안 검수(모바일 393px 기준)
- 다음 추가 후보: 정육 특화(고기 단면 클로즈업 축) · 카드뉴스 세로 스와이프 · 심야/타임세일 다크

---

## §8 0821 제작 화면 2차 개편 (Harold 접수 4건 — 코드 완료·배포 대기)

**접수**: ①전면개편이 덜 됐다(우측 열이 옛 form 그대로) ②상품 정보 수정이 없다 ③두꺼운 고딕(Black Han Sans)이 갑자기 나온다 — **무조건 Pretendard** ④가격 표기가 옆 카드와 통일이 안 된다. +"AI 제작툴 딱지를 떼라".

**원인(실측)**: ③ = §3의 0820 타이포 결정(화면 `.font-poster` + poster_pop·market_board·fresh_daily 3엔진). ④ = 가격 CSS를 엔진 11개가 각자 소유 + fresh_daily `.prc`가 `flex-wrap`이라 **가격 자릿수에 따라 원가·판매가가 한 줄/두 줄 오락가락**. ② = 손질 4종 철학으로 이름·판매가만, 그마저 수정 가능 표시 0.

**구현**:
- **서체**: 화면(index.html·index.css `--font-sans`·`.font-poster`) + 엔진 Black Han Sans 3종 + deal_feed Gasoek One 전부 → Pretendard(계층 = weight 900). 세리프(Hahmlet·Gowun — magazine·grid_muji 정체성)는 표제·본문에 한해 유지.
- **가격 공용 계약**: CT-F24 `priceStyleBlock()` 신설 → `renderTemplate` headInject에서 전 엔진 주입(bandStyleBlock과 같은 실측 셀렉터 패턴). 가격 숫자 = 전 엔진 Pretendard 900 tabular / 원가 취소선은 판매가 **위** 한 줄 고정(wrap 제거) / grid_hero·deal_bento는 판매가 아래 있던 원가 마크업을 위로 이동. `<html>`에 `data-engine` 속성 추가 + Pretendard 미로드 엔진 자동 주입.
- **상품 편집 시트**: 가격 키패드·이름 팝오버 2개 폐지 → 시트 1개(이름·판매가·원가·규격·원산지·뱃지 프리셋 4·이미지 자동 찾기/빼기 — auto-images 단건 재사용, 출처 표기·인쇄 관문 동의 축 그대로). 담긴 상품 행 = 썸네일+정보+가격(원가 취소선 포함)+연필. DDL 0·신규 endpoint 0.
- **AI 딱지 제거**: 다크 "AI 구성" 스트립 폐지 → 라이트 상태 바 1줄(점+상태+디자인 칩+바꾸기). 판정 근거 칩 삭제. "AI가 구성하는 중"→"전단을 만드는 중", "AI가 알아서"→"알아서 고르기", "AI 추천"→"추천". 히어로 잉크 그라데이션 텍스트·장식 격자 제거(격자는 미리보기 종이 바닥만 유지).

**검증**: backend tsc 0 · 게이트 99/99(엔진 실렌더 포함) · frontend `npm run build`(tsc -b) 통과 · Black Han Sans·Gasoek 잔존 grep 0 · 모델명 노출 grep 0.

**범위 밖 기록(미착수)**: POP·인쇄 렌더러의 Noto Sans KR → Pretendard 통일 / admin-frontend 서체 / 폐기 엔진 2종(magazine_zine·catalog_swipe — 옛 발행 URL 전용이라 미변경).

## §7 ⛔

- **엔진에 살아 있는 것을 화면에서 지우는 것은 단순화가 아니라 퇴보다.** 자동 선정을 얹더라도 고르는 자리는 남긴다.
- **한글 화면에 `word-break: keep-all`이 없으면 줄바꿈이 깨진다.** 문구를 줄이기 전에 이것부터.
- **이 프론트는 라이트 테마다.** 다크 클래스·유령 클래스(`text-text-tertiary`·`bg-surface-secondary`) 금지.
- **서버가 값을 덮어쓰면 화면의 선택은 없는 것과 같다.** 선택 축을 만들 때 저장·고정 경로까지 한 작업이다.
- 신규 엔진 추가 = 레지스트리 + RENDERER_MAP + **스모크 테스트 ENGINES 배열**까지가 한 세트.
- **(0821) 가격 표기의 서체·자세는 CT-F24 `priceStyleBlock()` 계약이 소유한다.** 엔진이 가격 숫자에 자기 서체를 다시 입히거나 원가·판매가 컨테이너를 `flex-wrap`으로 두지 않는다 — 자릿수 따라 줄이 붙었다 떨어지는 사고의 뿌리였다. 새 엔진의 가격 셀렉터는 계약 블록 주석의 실측 목록에 등재한다.
- **(0821) 화면·전단의 기본 서체는 Pretendard 단일이다.** Black Han Sans·Gasoek One류 블랙 고딕 재도입 금지(Harold 명시). 세리프는 magazine·grid_muji 표제·본문 정체성에 한해 허용 — 가격 숫자는 예외 없이 계약을 따른다.
